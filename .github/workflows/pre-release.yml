name: Pre-release

# Disabled - pre-release workflow is no longer needed.
# Official releases are now handled by build.yml.
# Keep this workflow for manual trigger only.

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    name: Pre-release Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Pre-release workflow has been disabled."
          echo "Please use the Build & Release workflow (build.yml) for official releases."